const arr=[2,3,-1,6]
const bubbleSort = (arr) =>{
    arr = [...arr]
    for (let i = 0; i < arr.length - i; i++) {
        arr.map((v,i,arr)=>{arr[i]>arr[i=1]})
        
    }
}
console.log(bubbleSort(arr));